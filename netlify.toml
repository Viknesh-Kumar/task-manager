[build]
  base = "."
  publish = "public"
  command = "echo 'Static site - no build needed'"

[context.production]
  command = "echo 'Static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
